server:
  port: 7000
  
spring:
  datasource: 
    url: jdbc:postgresql://my-database.cqoseij0k35s.us-east-2.rds.amazonaws.com/postgres
    username: postgres
    password: p4ssw0rd
    driver-class-name: org.postgresql.Driver
  jpa: 
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create
